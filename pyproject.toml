[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "football-predict-system"
version = "0.1.0"
description = "Football match outcome prediction system"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "fastapi>=0.100.0",
    "uvicorn[standard]>=0.23.0",
    "pydantic>=2.0.0",
    "pandas>=2.0.0",
    "numpy>=1.24.0",
    "scikit-learn>=1.3.0",
    "xgboost>=1.7.0",
    "psycopg2-binary>=2.9.0",
    "sqlalchemy>=2.0.0",
    "prefect>=2.0.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0.0",
    "pytest-cov>=4.0.0",
    "pytest-asyncio>=0.21.0",
    "ruff>=0.4.0",
    "mypy>=1.5.0",
    "bandit>=1.7.0",
    "pre-commit>=3.0.0",
    "types-setuptools",
    "diff-cover>=7.0.0",
]

[tool.hatch.build.targets.wheel]
packages = ["apps", "data_pipeline", "models", "trainer", "infra", "evaluation"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q -ra -m 'not slow'"
markers = [
    "slow: marks tests as slow (deselected by default)",
    "integration: marks tests as integration tests",
]

[tool.coverage.run]
source = ["."]
omit = ["tests/*", ".venv/*", "*/site-packages/*"]

[tool.coverage.report]
exclude_lines = ["pragma: no cover"]

[tool.ruff]
target-version = "py38"
line-length = 88

[tool.ruff.lint]
select = ["E", "F", "W", "I", "B", "UP", "RUF"]
ignore = ["E501", "B008", "B904"]

[tool.mypy]
python_version = "3.11"
warn_return_any = true
warn_unused_configs = true
disallow_untyped_defs = true
exclude = ["tests/", ".venv/", "scripts/"]

[[tool.mypy.overrides]]
module = [
    "prefect.*",
    "pandas.*", 
    "sklearn.*",
    "xgboost.*",
    "structlog.*",
    "httpx.*",
    "uvicorn.*",
    "fastapi.*",
    "pydantic.*"
]
ignore_errors = true
ignore_missing_imports = true
[[tool.mypy.overrides]]
module = ["prefect.*", "pandas.*", "sklearn.*", "xgboost.*"]
ignore_errors = true

[tool.bandit]
exclude_dirs = ["tests", ".venv"]
skips = ["B101", "B601"]
